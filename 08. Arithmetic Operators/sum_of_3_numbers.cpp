#include <iostream>
using namespace std;

int main()
{
    short a, b, c;
    cout << "Please enter the first number a: ";
    cin >> a;
    cout << "Please enter the first number b: ";
    cin >> b;
    cout << "Please enter the first number c: ";
    cin >> c;

    cout << "Sum of numbers = " << a + b + c << endl;

    return 0;
}